@model BlackJack.ViewModels.ResponseModel.ResponseGameListHistoryView

@{
    ViewBag.Title = "GameList";
}

<h3>@Model.Player.Name</h3>

<table class="table table-striped table-hover">
    <thead>
        <tr>
            <th>Game Number</th>
            <th>Amount Round</th>
        </tr>
    </thead>
    <tbody>

        @foreach (var a in Model.Games)
        {
            <tr>
                <td>@a.Number</td>
                <td>@a.AmountRounds</td>
                <td><a href="@Url.Action("RoundsList","History", new { id = a.Id, playerId = Model.Player.Id })">Details</a></td>
            </tr>
        }

    </tbody>
</table>