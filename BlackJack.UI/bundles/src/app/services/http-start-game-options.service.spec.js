import { TestBed } from '@angular/core/testing';
import { HttpStartGameOptionsService } from './http-start-game-options.service';
describe('HttpStartGameOptionsService', function () {
    beforeEach(function () { return TestBed.configureTestingModule({}); });
    it('should be created', function () {
        var service = TestBed.get(HttpStartGameOptionsService);
        expect(service).toBeTruthy();
    });
});
//# sourceMappingURL=http-start-game-options.service.spec.js.map